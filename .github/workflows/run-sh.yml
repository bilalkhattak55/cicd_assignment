name: Run on push

on: 
  push:


jobs:
    run-script:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Code
              uses: actions/checkout@v3

            - name: Run Shell Script
              run: ../run.sh